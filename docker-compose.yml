services:
  telegram-bidir-support-bot:
    build: .
    container_name: telegram-bidir-support-bot
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
